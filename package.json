{
  "name": "paringa-park-view",
  "version": "1.0.0",
  "description": "Paringa Park View Dairy Farm website",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./css/style.css --minify",
    "watch": "npx tailwindcss -i ./src/input.css -o ./css/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
